{
  "name": "trainmap",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "esbuild ./src/app.ts --bundle --minify --sourcemap --format=esm --platform=browser --outfile=public/app.js --keyfile=config/secret/key.pem --certfile=config/secret/cert.pem --watch --serve=127.0.0.1:3000 --servedir=public"
  },
  "author": "",
  "dependencies": {
    "@types/three": "^0.142.0",
    "date-fns": "^2.28.0",
    "esbuild": "^0.19.11",
    "three": "^0.142.0",
    "typescript": "^4.7.4"
  }
}
