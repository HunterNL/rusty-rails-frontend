{
  "name": "trainmap",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "esbuild ./src/app.ts --bundle --sourcemap --format=esm --platform=browser --outfile=public/app.js --keyfile=config/secret/key.pem --certfile=config/secret/cert.pem --watch --serve=127.0.0.1:3000 --servedir=public"
  },
  "author": "",
  "dependencies": {
    "date-fns": "^2.28.0",
    "three": "^0.152.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.11",
    "typescript": "^5.4.5",
    "@types/webxr": "^0.5.16",
    "@types/three": "^0.152.0"
  }
}